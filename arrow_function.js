// A function that checks if a number is even or odd.
check = (num) => num % 2 == 0 ? "Even" : "Odd";
console.log(check(0));

// A function that returns the square of a number.
square = (num) => num ** 2;
console.log(square(5));